import Hero from "@/components/main/Hero";


/**
 * Home page component that renders the main Hero section.
 *
 * Renders a root div containing the Hero component. This component accepts no props.
 *
 * @returns {JSX.Element} The home page content.
 */
export default function Home() {
  return (
    <div className="" >
        
        <Hero />
      

    </div>
  );
}
